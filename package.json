{
  "name": "wedguest",
  "private": true,
  "description": "Full-stack app: React frontend + Express backend",
  "scripts": {
    "dev": "npx --yes concurrently -n fe,be -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend",
    "start:backend": "npm start --prefix backend"
  }
}
